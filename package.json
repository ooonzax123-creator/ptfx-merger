{
  "name": "ptfx-merger",
  "version": "1.0.0",
  "description": "Merge multiple GTA V .ypt XML particle effect files into one custom dictionary",
  "type": "module",
  "scripts": {
    "merge": "bun src/merge.ts",
    "web": "bun src/server.ts",
    "dev": "bun --watch src/server.ts"
  },
  "dependencies": {
    "fast-xml-parser": "^4.4.1"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}
